requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.3  # BeautifulSoup's 'xml' parser (used for the PIB RSS feed) needs this
# Pinned exactly, not just >=: pypdf's text extraction changed between
# versions enough to change the SHA-256 of the *extracted text* for the
# same PDF bytes (pypdf 3.17.4 gave a different fingerprint than 6.19.0 for
# the same Act PDF) — an unpinned upgrade could make every source look
# "changed" overnight and burn the AI budget re-classifying nothing. Tested:
# reproduces the exact stored hashes for both the Act and Rules PDFs.
pypdf==6.19.0
python-dotenv==1.2.3
pyyaml==6.0.3
openpyxl==3.1.5
python-docx==1.2.0
anthropic==1.8.0
pytest==9.1.1
# Used by the one-time verbatim-rebuild/restoration scripts in scripts/
# (not by the daily pipeline itself, but needed for CI to run
# tests/test_legal_text_verbatim.py, which re-invokes them as a permanent
# check). pdfplumber is the primary extractor for the Act rebuild
# (PyMuPDF's native DLL was blocked by Windows Application Control on the
# machine that wrote it); pymupdf is used directly by the Rules
# corrigendum/restoration scripts, where it loads fine.
pdfplumber==0.11.10
pymupdf==1.28.2
